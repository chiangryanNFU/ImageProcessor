# 新功能：區域選取放大

## 功能說明

本次更新為 ImageProcessor 新增了區域選取放大功能，使用者可以在主視窗中拖曳選取圖片的任意區域進行放大查看，並在放大視窗中進行編輯。

## 主要功能

### 1. 區域選取 (Region Selection)
- **操作方式**：在主視窗中載入圖片後，按住滑鼠左鍵並拖曳即可選取區域
- **視覺回饋**：選取過程中會顯示藍色虛線矩形框，即時顯示選取範圍
- **自動開啟**：釋放滑鼠後，系統會自動開啟新視窗顯示放大的區域

### 2. 放大倍率設定 (Zoom Ratio Configuration)
- **設定位置**：工具選單 → 設定放大倍率
- **倍率範圍**：0.1x - 10.0x
- **預設值**：2.0x (兩倍放大)
- **持久性**：設定後的倍率會保留，直到下次修改

### 3. 放大視窗功能 (Zoom Window Features)

放大視窗提供以下兩個主要功能：

#### a. 另存新檔 (Save As)
- 點擊工具列的「另存新檔」按鈕
- 支援格式：PNG、JPG、BMP
- 可儲存放大後或編輯後的圖片

#### b. 畫筆工具 (Brush Tool)
- 點擊「畫筆工具」按鈕啟用/停用畫筆功能
- 啟用後游標變為十字型
- 可在圖片上自由繪製
- 畫筆設定：
  - 顏色：紅色
  - 大小：5 像素
  - 線條平滑處理

## 使用流程

1. **啟動程式**
   ```
   ./ImageProcessor
   ```

2. **載入圖片**
   - 點擊「開啟影像」或按 Ctrl+O
   - 選擇要處理的圖片檔案

3. **（可選）設定放大倍率**
   - 點擊工具列的「設定放大倍率」按鈕
   - 輸入期望的放大倍率（0.1 - 10.0）

4. **選取區域**
   - 在圖片上按住滑鼠左鍵
   - 拖曳至要選取的範圍
   - 釋放滑鼠

5. **在放大視窗中編輯**
   - 自動開啟放大視窗
   - 啟用畫筆工具進行標註或修改
   - 點擊「另存新檔」儲存結果

## 技術實作細節

### 新增檔案
- `zoomwindow.h` - ZoomWindow 類別定義
- `zoomwindow.cpp` - ZoomWindow 類別實作

### 修改檔案
- `ip.h` - 新增區域選取相關成員變數
- `ip.cpp` - 實作滑鼠拖曳選取和放大視窗開啟
- `ImageProcessor.pro` - 專案檔更新

### 關鍵技術點
1. **座標對應**：正確處理主視窗與圖片座標系的轉換
2. **邊界檢查**：確保選取區域不超出圖片範圍
3. **記憶體管理**：使用 Qt::WA_DeleteOnClose 自動管理視窗生命週期
4. **視覺回饋**：透過 paintEvent 繪製選取矩形框

## 注意事項

- 選取的區域寬度和高度必須大於 10 像素才會開啟放大視窗
- 畫筆繪製直接修改顯示的圖片，記得儲存以保留修改
- 可同時開啟多個放大視窗，每個視窗獨立運作
- 放大倍率設定會影響所有後續的區域選取操作

## 範例截圖

主視窗顯示新增的「設定放大倍率」功能按鈕：

![主視窗](https://github.com/user-attachments/assets/69627d1c-9631-476c-a5a9-75aad56bfaf3)
